import { Component } from "react";
import PropTypes from "prop-types";

export default class ActivityInfoComps extends Component {
  static propTypes = {
    icon: PropTypes.string,
  };

  render() {
    return <div></div>;
  }
}
